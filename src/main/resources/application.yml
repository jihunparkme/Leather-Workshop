server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8

spring:
  profiles:
    active: test
    group:
      test: test,oauth
      develop: develop,oauth,devdb
      production: production,oauth,proddb